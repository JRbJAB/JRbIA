project_id = "jrbia-platform"

[api]
enabled = true
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
major_version = 17

[auth]
enabled = true

# Hosted project: native Supabase Auth is the sole identity provider.
# Configure only reviewed redirect URLs. Do not commit project secrets.

[storage]
enabled = true
file_size_limit = "20MiB"
